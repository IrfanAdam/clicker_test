{
  "name": "test-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "gen:mechanics": "node scripts/generate-mechanics-graph.js",
    "verify:mechanics": "node scripts/generate-mechanics-graph.js --check"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}